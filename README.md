# Initial build initiated from my couch using AI Studio app

The original version was here:

View your app in AI Studio: https://ai.studio/apps/drive/1SfVaBojMKeT_p-xE8Zs1uCINSJkBI_B2

## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
3. Run the app:
   `npm run dev`
